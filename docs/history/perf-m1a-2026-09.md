# Перф-снимок M1a (2026-09)

Источник: `.superpowers/sdd/2026-09-07-m1a-static-pipeline/task-22-report.md` (Task 22,
раздел «Smoke test: measured time and memory»); цифры не переизмерялись в Task 23 — только
процитированы, как поручил контроллер (переизмерение стоило бы полного `cargo test`,
дешёвого шага в закрывающей сессии нет). Машина владельца, `/usr/bin/time -l`, фикстура —
1,000,049-байтный документ (`<!doctype html><body>` + повторяющийся `<div><p>lorem ipsum
dolor sit amet consectetur adipiscing elit</p></div>` × ~14 000, viewport по умолчанию
800×600).

## Время

| Профиль | Время (`cl-testshell render` целиком: html → dom → style → layout → paint → raster) |
|---|---|
| release | 1.51 с (после фикса культинга офскрин-айтемов — 1.53 с, см. отчёт Task 22 "Manual CLI reproduction of the fix") |
| debug (то, что реально гоняет `cargo test`) | ~4.7 с (три прогона: 4.70 с, 4.69 с, 4.75 с) |

Бюджет теста (`crates/testshell/tests/smoke.rs`): 5 с локально, 15 с в CI
(`CL_SMOKE_SLOW=1`). Оба профиля укладываются с запасом; debug — с запасом всего ~5–6% на
загруженной машине (см. отчёт Task 22, "Observed local flakiness").

## Память

Peak RSS (`maximum resident set size`): **~326–330 МиБ** (342–347 МБ в десятичной системе) —
**превышает** критерий плана «< 300 МБ» (`docs/superpowers/plans/2026-09-07-m1a-static-pipeline.md`,
раздел Verification, пункт 6). `peak memory footprint` (отдельная метрика macOS) — ~278–293 МБ,
укладывается.

Зафиксировано как невыполненный exit-критерий, не потихоньку проигнорировано:
`docs/PLAN.md` §M1a чекбокс не тикнут по этому пункту, `MEMORY.md` "Долги" содержит его.
Оптимизация памяти `cl-style`/`cl-layout` под этот бюджет не входила в мандат Task 22/23 —
следующий шаг по этому долгу увязан с M1e/будущей перф-задачей.

## Конформанс (для полноты снимка, из Task 20/8/6 report)

- html5lib-tests tree-construction: 1307/1313 = 99.5% (0 паник, порог был ≥ 90%).
- WPT `urltestdata.json`: 828/893 = 92.7% (порог ≥ 92%, потолок upstream `url`-крейта).
- Reftests (Task 23): 20/20 PASS (`cargo run -p cl-testshell -- reftest`).
- Fuzz-таргетов: 5 (`ipc_decode` из M0 + `html_parse`, `css_stylesheet`, `url_parse`,
  `display_list_validate` из M1a), по 60 с каждый без падений.
