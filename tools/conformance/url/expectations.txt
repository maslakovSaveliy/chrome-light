# Known-failing WPT url/resources/urltestdata.json cases for cl_net::Url.
#
# One entry per line: `<index>\t# reason`, where <index> is the 0-based position of the case
# in the raw JSON array (crates/net/tests/urltestdata.rs prints this index for every failure,
# and it is stable as long as urltestdata.json / PINNED_COMMIT do not change — a corpus refresh
# must regenerate this file, see README.md). Blank lines and full-line `#` comments are ignored.
# The harness asserts both directions: every case that actually fails must be listed here, and
# every case listed here must actually still fail (no stale entries).
#
# Real measured rate at PINNED_COMMIT 8fa79675278b21028654626c94dd7220f0f817a6: 828/893 (92.7%),
# The M1a gate is 0.92 (ADR ruling 2026-09-09: 0.95 was an estimate, 92.7% is the `url` crate ceiling). `cl_net::Url` is a thin newtype over the `url` crate 2.5.8 (idna
# 1.1.0), which does not itself pass 100% of this corpus — these 65 cases are genuine `url`
# crate gaps against the WHATWG URL Standard, not bugs introduced by this newtype. Cross-checked
# against servo/rust-url's own tests/expected_failures.txt (github.com/servo/rust-url,
# `url/tests/expected_failures.txt`): the file-url-slash-authority and opaque-percent-encoding
# categories have verbatim counterparts there. The idna-punycode category does NOT appear in that
# file — those 8 cases were verified only against the corpus itself and idna 1.1's own behaviour.
# Three root-cause categories, see the repeated reason text on each line below:
#   - file-url-slash-authority (47 cases): file: URLs / `///`-prefixed refs with irregular
#     slash counts or a windows drive-letter (`C:`, `C|`) segment in the authority or path.
#   - idna-punycode (8 cases): idna crate's ToASCII is stricter than WPT expects for labels
#     that already look like Punycode (`xn--...`).
#   - opaque-percent-encoding (10 cases): percent-encode-set / backslash-handling differences
#     for non-special (opaque-path) schemes.
# Fixing any of these means patching or replacing the `url`/`idna` crates upstream, which is
# out of scope for this task (see CODING_STANDARDS.md §10: no vendoring without an ADR). Report
# filed as DONE_WITH_CONCERNS; the M1a controller decides whether to lower the gate, patch
# `url`/`idna` upstream, or accept the deviation with a SPEC_REGISTRY row.
138	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
613	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
626	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
627	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
628	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
629	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
630	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
631	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
633	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
634	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
635	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
637	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
638	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
639	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
645	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
646	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
647	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
648	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
649	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
650	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
651	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
658	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
659	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
660	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
662	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
663	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
664	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
666	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
667	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
668	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
669	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
695	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
696	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
697	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
699	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
702	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
703	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
705	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
706	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
981	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
982	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
983	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
984	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
985	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
986	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
987	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
990	# file-url-slash-authority: url crate diverges from the WHATWG "special authority slashes state" / "shorten a url's path" algorithms for file: URLs (and `///`-prefixed relative refs against a special base) with extra/irregular leading slashes or a windows drive-letter segment (`C:`, `C|`) in the authority/path; known rust-url gap, see servo/rust-url tests/expected_failures.txt
310	# idna-punycode: idna crate 1.1.0's ToASCII validation rejects domain labels WPT expects to pass through (e.g. a bare `xn--` label, or a label that already contains `xn--...` but isn't valid Punycode) as "invalid international domain name"; known rust-url/idna strictness gap vs the WHATWG UTS-46 processing WPT expects
311	# idna-punycode: idna crate 1.1.0's ToASCII validation rejects domain labels WPT expects to pass through (e.g. a bare `xn--` label, or a label that already contains `xn--...` but isn't valid Punycode) as "invalid international domain name"; known rust-url/idna strictness gap vs the WHATWG UTS-46 processing WPT expects
313	# idna-punycode: idna crate 1.1.0's ToASCII validation rejects domain labels WPT expects to pass through (e.g. a bare `xn--` label, or a label that already contains `xn--...` but isn't valid Punycode) as "invalid international domain name"; known rust-url/idna strictness gap vs the WHATWG UTS-46 processing WPT expects
314	# idna-punycode: idna crate 1.1.0's ToASCII validation rejects domain labels WPT expects to pass through (e.g. a bare `xn--` label, or a label that already contains `xn--...` but isn't valid Punycode) as "invalid international domain name"; known rust-url/idna strictness gap vs the WHATWG UTS-46 processing WPT expects
315	# idna-punycode: idna crate 1.1.0's ToASCII validation rejects domain labels WPT expects to pass through (e.g. a bare `xn--` label, or a label that already contains `xn--...` but isn't valid Punycode) as "invalid international domain name"; known rust-url/idna strictness gap vs the WHATWG UTS-46 processing WPT expects
316	# idna-punycode: idna crate 1.1.0's ToASCII validation rejects domain labels WPT expects to pass through (e.g. a bare `xn--` label, or a label that already contains `xn--...` but isn't valid Punycode) as "invalid international domain name"; known rust-url/idna strictness gap vs the WHATWG UTS-46 processing WPT expects
839	# idna-punycode: idna crate 1.1.0's ToASCII validation rejects domain labels WPT expects to pass through (e.g. a bare `xn--` label, or a label that already contains `xn--...` but isn't valid Punycode) as "invalid international domain name"; known rust-url/idna strictness gap vs the WHATWG UTS-46 processing WPT expects
921	# idna-punycode: idna crate 1.1.0's ToASCII validation rejects domain labels WPT expects to pass through (e.g. a bare `xn--` label, or a label that already contains `xn--...` but isn't valid Punycode) as "invalid international domain name"; known rust-url/idna strictness gap vs the WHATWG UTS-46 processing WPT expects
292	# opaque-percent-encoding: percent-encode-set / backslash-handling differences for non-special (opaque-path) and `foo:`/`wss:` schemes — e.g. a literal space in an opaque path serializes without `%20`, `^` is not percent-encoded to `%5E` in the path percent-encode set, and a `\` after a non-special scheme incorrectly enters slash-handling state instead of staying a literal path character; known rust-url gap, see servo/rust-url tests/expected_failures.txt
293	# opaque-percent-encoding: percent-encode-set / backslash-handling differences for non-special (opaque-path) and `foo:`/`wss:` schemes — e.g. a literal space in an opaque path serializes without `%20`, `^` is not percent-encoded to `%5E` in the path percent-encode set, and a `\` after a non-special scheme incorrectly enters slash-handling state instead of staying a literal path character; known rust-url gap, see servo/rust-url tests/expected_failures.txt
296	# opaque-percent-encoding: percent-encode-set / backslash-handling differences for non-special (opaque-path) and `foo:`/`wss:` schemes — e.g. a literal space in an opaque path serializes without `%20`, `^` is not percent-encoded to `%5E` in the path percent-encode set, and a `\` after a non-special scheme incorrectly enters slash-handling state instead of staying a literal path character; known rust-url gap, see servo/rust-url tests/expected_failures.txt
297	# opaque-percent-encoding: percent-encode-set / backslash-handling differences for non-special (opaque-path) and `foo:`/`wss:` schemes — e.g. a literal space in an opaque path serializes without `%20`, `^` is not percent-encoded to `%5E` in the path percent-encode set, and a `\` after a non-special scheme incorrectly enters slash-handling state instead of staying a literal path character; known rust-url gap, see servo/rust-url tests/expected_failures.txt
298	# opaque-percent-encoding: percent-encode-set / backslash-handling differences for non-special (opaque-path) and `foo:`/`wss:` schemes — e.g. a literal space in an opaque path serializes without `%20`, `^` is not percent-encoded to `%5E` in the path percent-encode set, and a `\` after a non-special scheme incorrectly enters slash-handling state instead of staying a literal path character; known rust-url gap, see servo/rust-url tests/expected_failures.txt
854	# opaque-percent-encoding: percent-encode-set / backslash-handling differences for non-special (opaque-path) and `foo:`/`wss:` schemes — e.g. a literal space in an opaque path serializes without `%20`, `^` is not percent-encoded to `%5E` in the path percent-encode set, and a `\` after a non-special scheme incorrectly enters slash-handling state instead of staying a literal path character; known rust-url gap, see servo/rust-url tests/expected_failures.txt
855	# opaque-percent-encoding: percent-encode-set / backslash-handling differences for non-special (opaque-path) and `foo:`/`wss:` schemes — e.g. a literal space in an opaque path serializes without `%20`, `^` is not percent-encoded to `%5E` in the path percent-encode set, and a `\` after a non-special scheme incorrectly enters slash-handling state instead of staying a literal path character; known rust-url gap, see servo/rust-url tests/expected_failures.txt
1001	# opaque-percent-encoding: percent-encode-set / backslash-handling differences for non-special (opaque-path) and `foo:`/`wss:` schemes — e.g. a literal space in an opaque path serializes without `%20`, `^` is not percent-encoded to `%5E` in the path percent-encode set, and a `\` after a non-special scheme incorrectly enters slash-handling state instead of staying a literal path character; known rust-url gap, see servo/rust-url tests/expected_failures.txt
1002	# opaque-percent-encoding: percent-encode-set / backslash-handling differences for non-special (opaque-path) and `foo:`/`wss:` schemes — e.g. a literal space in an opaque path serializes without `%20`, `^` is not percent-encoded to `%5E` in the path percent-encode set, and a `\` after a non-special scheme incorrectly enters slash-handling state instead of staying a literal path character; known rust-url gap, see servo/rust-url tests/expected_failures.txt
1003	# opaque-percent-encoding: percent-encode-set / backslash-handling differences for non-special (opaque-path) and `foo:`/`wss:` schemes — e.g. a literal space in an opaque path serializes without `%20`, `^` is not percent-encoded to `%5E` in the path percent-encode set, and a `\` after a non-special scheme incorrectly enters slash-handling state instead of staying a literal path character; known rust-url gap, see servo/rust-url tests/expected_failures.txt
